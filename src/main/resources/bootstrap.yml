spring:
  application:
      name: telecom-platform
  cloud:
      config:
          uri: http://cloud-configuration-server.herokuapp.com

jasypt:
    encryptor:
        password: ${JASYPT_ENCRYPTOR_PASSWORD}